{
  "name": "web-fetch",
  "description": "Fetches a URL and returns the readable text content (HTML stripped). Use after web_search to read a specific result page in depth.",
  "version": "0.1.0",
  "parameters": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "The URL to fetch"
      }
    },
    "required": ["url"]
  },
  "env": ["HTTP_PROXY", "HTTPS_PROXY"]
}
